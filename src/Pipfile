[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
"discord.py" = {version = "==2.1.0", extras = ["speed", "voice"]}
"motor" = {version = ">=3.0.0", extras = ["srv"]}
lavalink = ">=3.1.4"
redis = ">=4.3.4"
jishaku = ">=2.5.1"
sentry-sdk = ">=1.9.7"
pytimeparse = ">=1.1.8"
GitPython = ">=3.1.27"
dataclasses-json = ">=0.5.7"

[dev-packages]
black = ">=22.8.0"

[requires]
python_version = "3.10"
