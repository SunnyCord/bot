version: '3.8'

services:

  lavalink:
    container_name: lavalink
    image: freyacodes:lavalink
    restart: always
    volumes:
      - ./application.yml:/opt/Lavalink/application.yml

  sunny:
    build: ./
    container_name: sunny
    volumes:
      - ./config.json:/home/appuser/config.json

networks:
  sunny:
